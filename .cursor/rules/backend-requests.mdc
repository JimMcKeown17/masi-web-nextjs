---
description: When and how to request new backend endpoints
globs:
alwaysApply: false
---

# Backend Endpoint Requests

## When to Request New Endpoints

Request backend endpoint when:
1. **Fetching >100 records** for processing
2. **Complex aggregations** (GROUP BY, COUNT, AVG across tables)
3. **Data joins** across multiple tables
4. **Performance-critical** operations
5. **Reusable calculations** needed by multiple components

## Frontend Processing is OK For

1. **UI State**: Filter selections, form state
2. **Small Datasets**: <100 items
3. **Client-Side Only**: Sorting/filtering already-fetched data
4. **Real-Time Updates**: Interactive chart filtering

## How to Request

1. **Document the need**: "Need monthly visit counts by program"
2. **Specify desired format**:
```json
{
  "data": [
    {
      "time_period": "2025-01",
      "literacy_visits": 45,
      "yebo_visits": 23
    }
  ]
}
```
3. **Suggest endpoint**: `GET /api/visit-frequency/`
4. **Specify query params**: `?time_filter=30days&school=10`
5. **Reference**: Check `recommended-backend-endpoints.md` first

## Current Priority Requests

See `cursor_project_rules/recommended-backend-endpoints.md` for:
- Priority 1: `/api/visit-frequency/` (with full Django code)
- Priority 2: `/api/school-performance/`
- Priority 3: `/api/mentor-activity/`
- Priority 4: `/api/program-trends/`

## Implementation Checklist

When backend endpoint is added:
- [ ] Update `cursor_project_rules/api-endpoints.md`
- [ ] Create TypeScript types in `/src/lib/types/`
- [ ] Create API function in `/src/lib/api/mentors/`
- [ ] Update frontend components
- [ ] Test with real data
